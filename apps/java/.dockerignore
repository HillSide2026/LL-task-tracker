# Exclude compiled artifacts — Maven rebuilds them inside the container.
# Without this, Docker sends hundreds of MB of bytecode as build context on every build.
**/target/

# IDE and OS noise
**/.idea/
**/.vscode/
**/.DS_Store
